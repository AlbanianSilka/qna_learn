  <%= simple_form_for([@post, @comment, @comment.answers.build]) do |f| %>
    <div class="field">
      <div class="control">
        <%= f.text_field :answer %>
      </div>
    </div>
    <div class="actions">
      <%= f.button :submit, 'Comment this answer', class: "button is-primary"%>
    </div>
  <% end %>
